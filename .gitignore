/node_modules
/.next
/out
/build
.DS_Store
*.pem
npm-debug.log*
.env*
/playwright-report
/test-results
/.vercel
next-env.d.ts

# Do NOT publish the paid product files to a public repo
*.pdf

# Local tooling / agent folders
/.claude
/.qodo
make_bundle.py

# Stray nested duplicate/backup of the project (contains PDFs) - do not publish
/DIABETES/

# Downloaded fonts for ad generation (re-downloadable, not source)
/_adfonts/
